# .github/workflows/daily-update.yml

name: Daily Market Update

on:
  schedule:
    - cron: "0 13 * * 1-5"  # Щодня о 13:00 UTC (8:00 NY)
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
    - name: Клон репозиторій
      uses: actions/checkout@v4

    - name: Встановлення Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Встановлення залежностей (якщо треба)
      run: pip install -r requirements.txt || true

    - name: Генерація аналітики
      run: python generate.py

    - name: Перевірка файлів після генерації
    - run: ls -1

    - name: Оновлення HTML
      run: python update.py

    - name: Коміт та пуш index.html
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "actions@github.com"
        git add index.html data/report.json
        git commit -m "🔄 Автоматичне оновлення index.html"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
